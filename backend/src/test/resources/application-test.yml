spring:
  datasource:
    url: jdbc:h2:mem:smart_expense_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: 01234567890123456789012345678901
    expiration: 60000
